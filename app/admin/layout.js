import "./admin.css";
export const metadata = {
  title: "Admin | Orlano Gardens",
  robots: { index: false, follow: false },
};

export default function AdminLayout({ children }) {
  return children;
}
