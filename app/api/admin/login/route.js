import {NextResponse} from "next/server"; import {ensureAdmin,verifyPassword,createSession} from "../../../../lib/auth";
export async function POST(req){try{const {email,password}=await req.json();const admin=await ensureAdmin();if(!admin||email.toLowerCase()!==admin.email||!(await verifyPassword(password,admin.passwordHash)))return NextResponse.json({error:"Invalid email or password"},{status:401});const res=NextResponse.json({ok:true});res.cookies.set("orlano_admin_session",createSession(admin),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:Number(process.env.SESSION_EXPIRES_HOURS||12)*3600});return res}catch(e){return NextResponse.json({error:e.message},{status:500})}}
